# Write your MySQL query statement below
select project_id,round(avg(experience_years),2) as average_years from (select p1.project_id,p1.employee_id,p2.experience_years from Project p1 left join Employee p2 on p1.employee_id=p2.employee_id) as table_one group by project_id; 